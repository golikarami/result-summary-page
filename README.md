# Result Summary Page 📊

A clean and responsive **Result Summary Page** built with **HTML and CSS**.  
This page displays summarized results, ideal for quizzes, tests, surveys, or simple scoreboards.

## Features
- Displays total score, percentages, or other key results
- Responsive layout for mobile and desktop
- Styled with modern CSS (colors, cards, typography)
- Simple, easy-to-read design

## Run

1. Clone the project:
```bash
git clone <https://github.com/golikarami/result-summary-page.git>
```
2. Go into the project folder:
```bash
cd result-summary-page
```
3. Open index.html in your browser (double-click or right-click → Open with → Browser).
